/* Lucide icon — rendered via SVG data URI */
:host {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
/* image rendered as block via host flex context */
.lucide-img {
  display: block;
}
